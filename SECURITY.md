# 安全政策

ClassCare 會處理學生、家庭聯絡、健康行動摘要與補助紀錄。任何正式資料都不得提交至此儲存庫、Issue、Pull Request、Actions log 或螢幕截圖。

## 專案狀態

- 安全與隱私規範：`DOCUMENTED`
- 程式碼中的權限與資料最小化機制：`IMPLEMENTED`
- 本機建置與語法檢查：依各次提交的測試紀錄判定是否為 `TESTED`
- Google Apps Script 實機驗證與正式部署：除非發布紀錄另有標示，否則不得視為 `TESTED` 或 `DEPLOYED`

## 回報安全問題

請勿在公開 Issue 中揭露弱點、帳號、學校名稱、學生資料、Spreadsheet ID、Folder ID、部署網址、存取權杖或重現用的正式資料。

建議透過 GitHub 儲存庫的 **Security → Report a vulnerability** 私密回報功能提交。若該功能尚未啟用，請停止公開揭露，改用專案負責人事先指定的校內私密管道聯絡。

回報時僅提供必要資訊：

- 受影響的檔案、函式與版本
- 使用完全虛構資料的重現步驟
- 權限或隱私影響
- 建議修補方向（若有）

## 憑證或資料誤提交

若發現敏感資訊已進入 Git 歷史：

1. 立即停用或輪替相關憑證與部署。
2. 限制儲存庫存取並通知專案負責人。
3. 依組織程序清理 Git 歷史；僅刪除工作目錄中的檔案並不足夠。
4. 檢查 GitHub Actions、Issue、PR 與快取是否留有副本。
5. 記錄事件與改善措施，但不得在紀錄中複製敏感值。
